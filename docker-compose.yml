services:
  autoupdate:
    build: .
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      CODEX_EXEC_PATH: codex
    volumes:
      - ./runs:/app/runs
    command:
      - --run-name
      - docker-full-refresh
      - --skip-scraper-install
      - --skip-playwright-install
