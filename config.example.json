{
  "purpose": "Ejemplo documental. El runner actual se configura por parametros PowerShell.",
  "autonomousMode": {
    "description": "Usar scraper integrado + Pipeline Sagrado completo.",
    "command": "node .\\run_auto_update.mjs --run-name daily-full-refresh",
    "scraper": {
      "connector": "all",
      "meaning": "all = vaperalia + eciglogistica",
      "concurrency": 5,
      "limit": 0
    }
  },
  "inputJsonMode": {
    "description": "Usar un JSON ya scrapeado.",
    "command": "node .\\run_auto_update.mjs --input-json \"C:\\\\ruta\\\\output.json\""
  },
  "externalScraperMode": {
    "description": "Usar un scraper externo. El comando debe escribir el JSON final en $env:AUTOUPDATE_SCRAPE_OUTPUT_JSON.",
    "command": "node .\\run_auto_update.mjs --scraper-command \"cd C:\\\\ruta\\\\scraper && npm start\""
  },
  "codexExec": {
    "batchSize": 20,
    "decisionContract": ["accepted", "rejected"],
    "needsHumanAllowed": false
  }
}
